{% extends 'base.html.twig' %}

{% block title %}Форум{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-12">
            {% for post in posts %}

                <div class="pb-4 post-grid">
                    <div>
                        <a class="more-info"
                           href="{{ path('app_show_post',{'id': post.id}) }}">{{ post.title }}</a>
                    </div>
                    <div>
                       <b>{{ post.user.username }}</b> / ответов: <b>{{ post.getSubPostsCount }}</b>
                    </div>
                </div>

            {% endfor %}


        </div>
    </div>

{% endblock %}
